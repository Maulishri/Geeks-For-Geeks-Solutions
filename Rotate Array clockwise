#include <iostream>
using namespace std;


int main() {
    
    int k;
    cin>>k;
    while(k>0)
    {
        int l,d;
        cin>>l>>d;
        int a[l];
        int arr[l];
        for(int i=0; i<l; i++)
            cin>>a[i];
        for(int i=0; i<l; i++)
            arr[i] = a[(i+d)%l];
        
        for(int i=0; i<l; i++)
            cout<<arr[i]<<" ";
        k--;
        cout<<endl;
    }
    
	//code
	return 0;
}
